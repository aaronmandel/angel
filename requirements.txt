# Core Bot Dependencies
discord.py==2.3.2
python-dotenv==1.0.1

# Google Sheets API
gspread==6.0.2
oauth2client==4.1.3

# OpenAI (latest compatible)
openai==1.12.0
httpx==0.27.0

# Natural Language Date Parsing
dateparser==1.1.8

# Uvicorn (not used, but harmless if kept)
uvicorn==0.29.0
